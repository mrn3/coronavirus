{
  "development": {
    "dialect": "postgres",
    "host": "localhost",
    "port": 5432,
    "database": "secondmetrics",
    "operatorsAliases": {
      "$and": "Op.and",
      "$or": "Op.or",
      "$eq": "Op.eq",
      "$gt": "Op.gt",
      "$lt": "Op.lt",
      "$lte": "Op.lte",
      "$like": "Op.like"
    }
  },
  "production": {
    "dialect": "postgres",
    "host": "localhost",
    "port": 5432,
    "database": "secondmetrics",
    "username": "root",
    "password": null,
    "operatorsAliases": {
      "$and": "Op.and",
      "$or": "Op.or",
      "$eq": "Op.eq",
      "$gt": "Op.gt",
      "$lt": "Op.lt",
      "$lte": "Op.lte",
      "$like": "Op.like"
    }
  }
}